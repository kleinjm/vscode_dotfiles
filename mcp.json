{
  "mcpServers": {
    "playwright": {
      "command": "node",
      "args": ["./node_modules/@playwright/mcp/lib/program.js", "--isolated", "--headless", "--no-sandbox", "--browser", "chromium"]
    }
  }
}
